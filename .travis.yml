language: python
dist: xenial
python:
    - "3.7"
    - "3.6"
sudo: required
before_install:
    - pip install --upgrade pip
install:
    - make build
    - pip install codecov
script:
    - make test
    - coverage run -m unittest tests/*.py
after_success:
    - codecov
